@import "bootstrap/variables"

<% require 'bootstrap-square/version' %>
// Override Bootstrap variables here (defaults from bootstrap-square v<%= Bootstrap::VERSION %>):

<%= File.read(@template[:options][:bs_variables_path]).gsub(/^(?=\$)/, '// ').gsub(/ !default;/, '') %>
